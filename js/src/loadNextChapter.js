/*global define, require, debug*/
define(['jquery', 'bible', 'external/jquery.waypoints', 'jquery-mobile', 'ba-debug'], function ($, bible) {
	"use strict";
	/*$(document).bind('attachWaypoints', function () {
		$('.loadNextChapter').waypoint(function (event, direction) {
			//debug.debug(direction);
			//console.log($(this));
			//event.stopPropagation();
		});
	});
	$('#reference').find('[data-role=content]').bind('scrollstop', function (event) {

		//What occurs when the scrolling starts
		debug.debug(event);
		debug.debug($(this).scrollTop());

	});*/
	/*$('#reference').find('[data-role=content]').bind('scrollstart', function (event) {

		//What occurs when the scrolling starts
		debug.debug('You have stopped scrolling');

	});
	setInterval(function () {
		debug.debug('i');
	}, 1000);*/
});