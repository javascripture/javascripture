/*global define, $, module, test, ok, debug */
define(['../external/qunit', 'ba-debug', 'src/keyboardShortcuts'], function () {
	"use strict";
	module("keyboardShortcuts");

	test("goto", function () {
		ok();
	});
});